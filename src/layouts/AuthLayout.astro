---
import Layout from "../layouts/Layout.astro";
import Image from "astro/components/Image.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <main id="main">
    <div class="grid">
      <div id="orange-upper-section">
        <Image
          width={200}
          height={294.62}
          loading={"lazy"}
          src="/public/big-logo.svg"
          alt="big-logo"
          id={"big-logo"}
        />
        <p id="login-big-text">
          Lorem ipsum dolor sit amet consectetur. At nunc massa sed eleifend.
          Facilisis dolor sem amet purus sagittis odio. Adipiscing tempus nibh
          enim est arcu diam.
        </p>
        <p id="login-small-text">
          Cursus vehicula orci phasellus cum nullam sagittis sagittis varius.
          Non orci convallis arcu ultrices aliquam senectus. Mauris vitae mi id
          a posuere in id. Nulla sit nisl magna ligula nisl. Sit nibh tristique
          semper
        </p>
      </div>
      <div class="lower-section">
        <form onsubmit="" id="form">
          <slot class="slot" />
        </form>
      </div>
    </div>
  </main>
  <style>
    #main {
      height: 100vh;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      background: var(--BACKGROUND, #f5faf9);
    }
    .lower-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 3.1rem;
    }
    #orange-upper-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 11rem 6.875rem 11rem 7rem;
      background: var(--PRIMARY, #f36337);
    }
    #login-big-text {
      color: var(--BACKGROUND, #f5faf9);
      text-align: center;

      /* HEADING_3 */
      font-size: 2rem;
      font-style: normal;
      font-weight: 400;
      line-height: 110%; /* 2.2rem */
    }
    #login-small-text {
      color: var(--BACKGROUND, #f5faf9);

      /* BODY_EXTRA_BIG_BOLD */
      font-size: 1.125rem;
      font-style: normal;
      font-weight: 400;
      line-height: normal;
    }
    #form {
      display: flex;
      flex-direction: column;
    }
    @media (max-width: 700px) {
      .grid {
        display: flex;
        justify-content: center;
        height: 100vh;
        /*/margin: 5.62rem 0;/*/
      }
      #orange-upper-section {
        display: none;
      }
      .column {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        gap: 3.1rem;
      }
    }
  </style>
</Layout>
