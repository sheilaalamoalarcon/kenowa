export enum Routes {
  LOG_OUT = "/logOut",
  LOG_IN = "/logIn",
  FEED = "/feed",
  PROFILE = "/profile",
  LANDING = "/",
}

export enum AlertType {
  SUCCESS,
  INFO,
  WARNING,
  ERROR,
}
export enum StylesTypes {
  REGULAR = "regular",
  ITALIC = "italic",
  BOLD = "bold",
  UNDERLINE = "underlined",
}
