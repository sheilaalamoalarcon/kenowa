export enum Routes {
  LOG_OUT = "/logOut",
  LOG_IN = "/logIn",
  FEED = "/feed",
  PROFILE = "/profile",
  LANDING = "/",
}

export enum AlertType {
  SUCCESS,
  INFO,
  WARNING,
  ERROR,
}
export enum StylesTypes {
  ITALIC = "italic",
  BOLD = "bold",
  UNDERLINE = "underline",
}
